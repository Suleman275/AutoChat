<html lang="en">

<head>
   <meta charset="UTF-8">
   <meta name="viewport" content="width=device-width, initial-scale=1.0">
   <title>AutoChat | <%= title %>
   </title>
   <link rel="stylesheet" href="/styles.css">
</head>

<body>
   <nav>
      <div>
         <h1><a href="/dashboard">AutoChat</a></h1>
      </div>
      <div>
         <% if (user) { %>
            Welcome, <%= user.email %>
               <a href="/logout">Log out</a>
               <% } else { %>
                  <a class="white-hover" href="login">Log In</a>
                  <a class="white-hover btn-highlight" href="signup">Sign Up</a>
                  <% } %>
      </div>
   </nav>